[
  {
    "task": "Create a regular expression to validate an AWS IAM username, which must start with a letter, be 1-64 characters long, and only contain letters, numbers, and underscores",
    "format": "regex",
    "solution_criteria": "Regex should enforce username rules: starts with letter, allows 1-64 characters, only letters/numbers/underscores"
  },
  {
    "task": "Write a Python function that converts an AWS EC2 instance type (e.g. 't2.micro') into a dictionary with separate size and generation components",
    "format": "python",
    "solution_criteria": "Function should parse instance type string into dictionary with 'generation' and 'size' keys"
  },
  {
    "task": "Create a JSON object representing the basic configuration for an AWS S3 bucket policy that allows public read access",
    "format": "json",
    "solution_criteria": "JSON must include valid policy structure with a statement allowing public read access to bucket"
  }
]